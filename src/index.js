// Ao innicializar o projeto com o npm start o React irá procurar o arquivo "index.js" para inicializar o app
import ReactDOM from 'react-dom';
import App from './App';
import './styles/global.css'

ReactDOM.render(
<App />, 
document.getElementById('root'),
);